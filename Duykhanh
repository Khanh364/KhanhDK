local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "KhanhUltimate",
    SubTitle = "2026",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Light",
    MinimizeKey = Enum.KeyCode.RightControl
})

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "home" }),
    Farm = Window:AddTab({ Title = "Farm", Icon = "swords" }),
    Misc = Window:AddTab({ Title = "Misc", Icon = "settings" })
}

-- Nút bật/tắt (đã đổi sang PlayerGui để dễ hiện hơn)
local lp = game.Players.LocalPlayer
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ProToggle"
ScreenGui.Parent = lp:WaitForChild("PlayerGui")  -- Fix: dùng PlayerGui thay CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 999

local btn = Instance.new("TextButton")
btn.Parent = ScreenGui
btn.BackgroundColor3 = Color3.fromRGB(0,0,0)
btn.BackgroundTransparency = 0.3
btn.Position = UDim2.new(0,10,0.5,0)
btn.Size = UDim2.new(0,60,0,60)
btn.Text = "Pro"
btn.TextColor3 = Color3.fromRGB(0,255,0)
btn.TextSize = 20
btn.Font = Enum.Font.SourceSansBold
btn.Draggable = true
btn.Active = true

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0,12)
corner.Parent = btn

btn.MouseButton1Click:Connect(function()
    local vim = game:GetService("VirtualInputManager")
    vim:SendKeyEvent(true, Enum.KeyCode.RightControl, false, game)
    task.wait(0.03)
    vim:SendKeyEvent(false, Enum.KeyCode.RightControl, false, game)
end)

Window:SelectTab(1)

print("Nút Pro đã tạo - kiểm tra PlayerGui hoặc màn hình!")
