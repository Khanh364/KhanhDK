local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Khánh Hub Ultimate 🍉",
    SubTitle = "2026",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Light",
    MinimizeKey = Enum.KeyCode.RightControl
})

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "home" }),
    Farm = Window:AddTab({ Title = "Farm", Icon = "swords" }),
    Misc = Window:AddTab({ Title = "Misc", Icon = "settings" })
}

local lp = game.Players.LocalPlayer
getgenv().Config = {}

local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    if getgenv().Config.FastAttack and getnamecallmethod() == "FireServer" and self.Name == "MeleeDamage" then
        for i = 1, 5 do oldNamecall(self, ...) end
    end
    return oldNamecall(self, ...)
end)
setreadonly(mt, true)

Tabs.Main:AddToggle("FA", {Title = "Fast Attack", Default = false, Callback = function(v) getgenv().Config.FastAttack = v end})
Tabs.Farm:AddToggle("AF", {Title = "Auto Farm", Default = false, Callback = function(v) getgenv().Config.AutoFarm = v end})

local ScreenGui = Instance.new("ScreenGui")
local TextButton = Instance.new("TextButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Name = "SkibidiToggle"
ScreenGui.Parent = (game:GetService("CoreGui"):FindFirstChild("RobloxGui") or game:GetService("CoreGui"))
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 9999

TextButton.Parent = ScreenGui
TextButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
TextButton.BackgroundTransparency = 0.4
TextButton.Position = UDim2.new(0, 20, 0.4, 0)
TextButton.Size = UDim2.new(0, 80, 0, 35)
TextButton.Text = "skibidi"
TextButton.TextColor3 = Color3.fromRGB(255, 255, 255)
TextButton.TextSize = 16
TextButton.Font = Enum.Font.GothamBold
TextButton.Active = true
TextButton.Draggable = true

UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = TextButton

-- Fix triệt để: Gọi hàm Close/Open của chính Window Fluent
TextButton.MouseButton1Click:Connect(function()
    if Window then
        Window:Minimize()
    end
end)

Window:SelectTab(1)
